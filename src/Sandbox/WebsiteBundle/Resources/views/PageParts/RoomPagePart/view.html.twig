<div class="room-pp">
    <div class="row">
        <div class="col-md-12">
            <p>{{ resource.name }}</p>
            {% if resource.image %}
                <img src="{{ resource.image }}" class="img-responsive"/>
            {% endif %}
            <p>{{ resource.content|raw }}</p>
        </div>
    </div>
</div>