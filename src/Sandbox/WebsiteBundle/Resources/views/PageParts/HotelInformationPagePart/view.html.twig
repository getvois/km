<div class="hotel-information-pp">
    <p>{{ resource.name }}</p>
    <p>{{ resource.description|raw }}</p>
    {% if resource.images %}
        {% for image in resource.images %}
            <img src="{{ image }}" class="img-responsive"/>
        {% endfor %}
    {% endif %}
</div>