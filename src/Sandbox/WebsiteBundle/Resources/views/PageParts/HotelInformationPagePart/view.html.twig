<div class="hotel-information-pp">
    <div class="row">
        <div class="col-md-12">
            {% if resource.description or resource.images|length > 0 %}
                <p><span class="fa fa-angle-double-right"></span>&nbsp;
                    <a class="" data-toggle="collapse" href="#collapse-{{ resource.id }}" aria-expanded="false" aria-controls="collapse-{{ resource.id }}">
                       {% if resource.nameTranslated %}
                            {{ resource.nameTranslated }}
                        {% else %}
                            {{ resource.name }}
                        {% endif %}
                    </a>
                </p>
                <div class="collapse" id="collapse-{{ resource.id }}">
                    <p>{{ resource.description|raw }}</p>
                    {% if resource.images|length > 0 %}
                        <div class="row">
                            {% for image in resource.images %}
                                <div class="col-md-3">
                                    <img src="{{ image.imageUrl }}" class="img-responsive"/>
                                </div>
                            {% endfor %}
                        </div>
                    {% endif %}
                </div>
            {% else %}
                <p>
                    <span class="fa fa-angle-double-right"></span>&nbsp;
                    {% if resource.nameTranslated %}
                        {{ resource.nameTranslated }}
                    {% else %}
                        {{ resource.name }}
                    {% endif %}
                </p>
            {% endif %}


        </div>
    </div>
</div>