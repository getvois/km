
<nav class="navbar navbar-default">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            {#<a class="navbar-brand" href="#">Brand</a>#}
        </div>


        <div class="collapse navbar-collapse site-topmenu" id="bs-example-navbar-collapse-1">
            {#<ul class="nav navbar-nav">#}
                {#<li class="active"><a href="#">Link <span class="sr-only">(current)</span></a></li>#}
                {#<li><a href="#">Link</a></li>#}
            {#</ul>#}
            <ul class="nav navbar-nav travelbase_top_menu">

                {# COMPANIES AND DESTINATION FROM TOP MENU #}
                {#<li>#}
                    {#<a href="#" onclick="return false;">#}
                        {#<span class="city-picker-list city-picker-dropdown" data-target="#dropdown-departure">{{ 'Destinations'|trans({}, 'frontend', app.request.locale) }}</span>#}
                    {#</a>#}
                {#</li>#}
                {#<li>#}
                    {#<a href="#" onclick="return false;">#}
                        {#<span class="city-picker-list city-picker-dropdown" data-target="#dropdown-company">{{ 'Companies'|trans({}, 'frontend', app.request.locale) }}</span>#}
                    {#</a>#}
                {#</li>#}

                {% if pages is defined %}
                    {% for page in pages %}
                        <li><a href="{{ path("_slug", { 'url': get_node_translation_for(page).fullslug}) }}">{{ page.title }}</a></li>
                    {% endfor %}
                {% endif %}
            </ul>
        </div><!-- /.navbar-collapse -->



</nav>
