{% if app.user %}

	<p>{{ 'club.logged.description' | trans({}, 'frontend') }}</p>

    <h4><a data-toggle="modal" data-target="#modal-user-data" id="open-user-tab" href="#">{{ app.user.email }} <span class="fa fa-edit"></span></a></h4>
    {#{{ render(controller('SandboxWebsiteBundle:Subscription:subscribeTree')) }}#}

    <a class="btn btn-default" href="{{ path('logout_action') }}">{{ 'Logout'|trans({}, 'frontend') }}</a>
{% else %}

    <div id="club-collapse">

    <h3><a href="#" data-toggle="modal" data-tab="register-form" data-target="#modal-registration">{{ 'subscribe.now'|trans({}, 'frontend') }}</a></h3>
    {#<h3><a href="#" data-toggle="modal" data-tab="login-form" data-target="#modal-registration">{{ 'login.here'|trans({}, 'frontend') }}</a></h3>#}
    <p>{{ 'subscribe.now.text'|trans({}, 'frontend') }}</p>
	
		<form action="{{ path('login_action') }}" method="post">
        {#<div class="row">#}
			{#<div class="col-md-9 col-xs-9">#}
		        {#<div class="form-group">#}
		            {#<label class="sr-only" for="email">{{ 'Email'|trans({}, 'frontend') }}:</label>#}
		            {#<input required id="email" type="text" name="email" class="form-control" placeholder="{{ 'Email'|trans({}, 'frontend') }}"/>#}
		        {#</div>#}
		{##}
		        {#<div class="form-group">#}
		            {#<label class="sr-only" for="password">{{ 'Password'|trans({}, 'frontend') }}:</label>#}
		            {#<input required id="password" type="password" name="password" class="form-control" placeholder="{{ 'Password'|trans({}, 'frontend') }}"/>#}
		        {#</div>#}
	 		{#</div>#}
	 	{#</div>#}
		<div class="row">
			<div class="col-md-12 col-xs-12">
		        <p>

                    <a class="btn btn-primary" href="#" data-toggle="modal" data-tab="login-form" data-target="#modal-registration">{{ 'sign.up'|trans({}, 'frontend') }}</a>
                    {#<input type="submit" class="btn btn-primary" value="{{ 'Login'|trans({}, 'frontend') }}"/>#}
		            <a class="fb-login-btn btn btn-primary" href="#"><span class="fa fa-facebook"></span> | {{ 'FB.login'|trans({}, 'frontend') }}</a>
		
		            {% if host and host.vkAppId and host.vkAppSecret %}
		                <a href="#" id="vk-login-btn" class="btn btn-primary"><span class="fa fa-vk"></span> | login</a>
		                <script>
		                    $("#vk-login-btn").click(function () {
		                        window.open('https://oauth.vk.com/authorize?client_id={{ host.vkAppId }}&scope=email&redirect_uri={{ app.request.schemeAndHttpHost }}/vk-login&response_type=code&v=5.29&state=');
		                    });
		                </script>
		            {% endif %}
		        </p>
	        </div>
		</div>
    	</form>

    </div>
    

{% endif %}